# -----------------------------
# Application Configuration
# -----------------------------

# Port where your Express server will run
PORT=5000

# MongoDB connection string
# Replace <DB_HOST>, <DB_PORT>, and <DB_NAME> with actual values
MONGO_URI=mongodb://<DB_HOST>:<DB_PORT>/<DB_NAME>

# Environment type
# Options: development | production | test
NODE_ENV=development

# The URL of your frontend application (used for CORS, cookies, redirects, etc.)
FRONTEND_URL=http://localhost:3000
